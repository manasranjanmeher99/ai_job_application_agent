# Resume Bullet Points

Pick 2-4 depending on space; adjust metrics/numbers to your actual usage.

**Option A — Concise, general:**
- Built and deployed an end-to-end GenAI web app (Streamlit + Google Gemini)
  that scores resume-to-job-description fit using TF-IDF/cosine similarity
  and a 100+ term skill taxonomy, and auto-generates tailored cover letters,
  resume edits, and interview prep.

**Option B — Emphasize NLP/ML:**
- Designed a hybrid resume-matching algorithm combining TF-IDF cosine
  similarity with rule-based skill-taxonomy extraction across 8 skill
  categories (ML, cloud, data engineering, BI, etc.), improving match
  transparency over black-box similarity scores.

**Option C — Emphasize engineering/reliability:**
- Architected a fail-safe LLM integration layer (Google Gemini API) with
  deterministic template fallbacks, ensuring 100% feature availability
  offline or without API credentials — validated with a 20+ test pytest
  suite covering parsing, scoring, and persistence.

**Option D — Emphasize product thinking:**
- Shipped a full job-search workflow tool (resume parsing → match scoring →
  skill-gap/ATS keyword analysis → AI content generation → SQLite tracker),
  reducing the manual effort of tailoring a job application from ~30 minutes
  to under 5.

**Option E — One-liner for a skills/projects section:**
- **AI Job Application Agent** (Python, Streamlit, scikit-learn, Gemini API,
  SQLite) — GenAI tool that matches resumes to jobs, detects skill/ATS gaps,
  and generates tailored cover letters and interview prep. [GitHub link]
